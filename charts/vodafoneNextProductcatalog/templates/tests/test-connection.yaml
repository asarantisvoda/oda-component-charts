apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next-helm.fullname" . }}-test-connection"
  labels:
    {{- include "next-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
