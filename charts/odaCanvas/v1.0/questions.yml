categories:
    - oda
    - canvas
    questions:
    - variable: persistence.enabled
    default: "false"
    description: "Enable persistent volume for Canvas"
    type: boolean
    required: true
    label: Canvas Persistent Volume Enabled
    show_subquestion_if: true
    group: "Canvas Settings"
    subquestions:
    - variable: persistence.size
        default: "10Gi"
        description: "Canvas Persistent Volume Size"
        type: string
        label: Canvas Volume Size
    - variable: persistence.storageClass
        default: ""
        description: "If undefined or null, uses the default StorageClass. Default to null"
        type: storageclass
        label: Default StorageClass for Canvas